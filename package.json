{
  "name": "tsx-control-statements",
  "version": "1.0.0",
  "main": "transform.js",
  "ts-main": "transform.ts",
  "author": "Konstantin Simeonov <kon.simeonov@protonmail.com>",
  "license": "MIT",
  "types": "index.ts",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc --project ./tsconfig.json",
    "test": "yarn && yarn build && cd test && yarn && yarn build && yarn test"
  },
  "devDependencies": {
    "@types/node": "^9.6.0",
    "typescript": ">=2.3.0"
  }
}
